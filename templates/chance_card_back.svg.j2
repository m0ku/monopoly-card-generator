<svg width="{{ width }}" height="{{ height }}" viewBox="0 0 {{ width }} {{ height }}" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
    <style>
        @font-face {
            font-family: 'Monopoly';
            src: url(data:font/woff2;base64,{{ font_b64 }}) format('woff2');
        }
        .card-title { font-family: 'sans-serif'; font-size: {{ title_size }}px; font-weight: bold; }
        .card-text { font-family: 'sans-serif'; font-size: {{ font_size }}px; font-weight: bold; }
    </style>

    <!-- Hintergrund -->
    <rect width="100%" height="100%" fill="{{ color }}" stroke="black" stroke-width="2"/>

    <!-- Background image -->
    <!-- image x="0" y="0" width="100%" height="100%" href="data:image/svg+xml;base64,{{ img_b64 }}" /-->
    
    <!-- Title -->
    <text x="50%" y="50%" text-anchor="middle" class="card-title">EREIGNISKARTE</text>
</svg>