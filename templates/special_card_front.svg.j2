<svg width="{{ width }}" height="{{ height }}" viewBox="0 0 {{ width }} {{ height }}" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
    <style>
        @font-face {
            font-family: 'Monopoly';
            src: url(data:font/woff2;base64,{{ font_b64 }}) format('woff2');
        }
        .card-title { font-family: 'sans-serif'; font-size: {{ title_size }}px; font-weight: bold; }
        .card-text-bold { font-family: 'sans-serif'; font-size: {{ font_size }}px; font-weight: bold; }
        .card-text { font-family: 'sans-serif'; font-size: {{ font_size }}px;}
        .card-text-small { font-family: 'sans-serif'; font-size: {{ font_size - 1 }}px;}
    </style>

    <!-- 1. Äußerer Kartenrand -->
    <rect width="100%" height="100%" fill="white" stroke="black" stroke-width="0.5" stroke-dasharray="5, 5"/>
    
    <!-- 2. Innerer Rahmen -->
    <!-- rect x="{{margin}}" y="{{margin}}" width="{{inner_width}}" height="{{inner_height}}" fill="none" stroke="black" stroke-width="1"/ -->
    
    <!-- Bild -->
    <svg x="30%" y="3%"
        width="{{ width*0.4 }}"
        height="{{height*0.4}}"
        viewBox="0 0 {{ width * 0.4 }} {{ height * 0.4 }}">
        <image width="100%" height="100%" preserveAspectRatio="xMidYMid meet" href="data:image/svg+xml;base64,{{ img_b64 }}" />
    </svg>

    <!-- Titel -->
    <text x="50%" y="{{title_y}}" text-anchor="middle" class="card-text-bold" fill="black" style="text-transform: uppercase;">BESITZRECHTKARTE</text>
    <text x="50%" y="{{title_y + 35}}" text-anchor="middle" class="card-title" fill="black" style="text-transform: uppercase;">{{title.upper()}}</text>
    
    <!-- Mietentabelle -->
    <g transform="translate({{margin + 15}}, {{rect_y + header_h + 35}})" class="card-text-bold" font-size="13">
        <line x1="0%" y1="0" x2="84%" y2="0" style="stroke:black;stroke-width:1.5"/>
        <text x="0" y="20" class="card-text-small">
            <tspan x="40%" dy="0" text-anchor="middle">Wenn man ein Versorgungswerk</tspan>
            <tspan x="40%" dy="1.2em" text-anchor="middle">besitzt, ist die Miete 4-mal so hoch</tspan>
            <tspan x="40%" dy="1.2em" text-anchor="middle">wie der Würfelwurf.</tspan>
            <tspan x="40%" dy="2.2em" text-anchor="middle">Wenn man beide Versorgungswerke</tspan>
            <tspan x="40%" dy="1.2em" text-anchor="middle">besitzt, ist die Miete 10-mal so hoch</tspan>
            <tspan x="40%" dy="1.2em" text-anchor="middle">wie der Würfelwurf.</tspan>
        </text>
    </g>

</svg>